{% extends 'championship/round/_layout.html.twig' %}

{% set activeTab = 'maps' %}

{% block tab_content %}
    {% if not round.areMapsRevealed %}
        <div class="bg-bg-card rounded-lg p-8 border border-metal-dark text-center">
            <p class="text-gray-mid">{{ 'championship.maps_hidden'|trans({}, 'championship') }}</p>
        </div>
    {% elseif round.maps is empty %}
        <div class="bg-bg-card rounded-lg p-8 border border-metal-dark text-center">
            <p class="text-gray-mid">{{ 'championship.no_maps'|trans({}, 'championship') }}</p>
        </div>
    {% else %}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {% for map in round.maps %}
                {% include 'components/_map_card.html.twig' with {map: map, mapRecords: mapRecords} %}
            {% endfor %}
        </div>
    {% endif %}
{% endblock %}
